Header text - handle element generation side effects - implemented
Image: - implemented
	-Include sample images
	-Image component
Navbar: - implemented
	-Where users can select their component choice with a single click

LinkView:
	-A concept where you connect to database, store the content with its corresponding user id. The linkview will contain an encrypted user id, which you will decrypt, connect to database, obtain the content, and display it on the webpage. Also if current link has a linkview, the viewer cannot edit the content.
